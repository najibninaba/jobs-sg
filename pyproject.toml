[project]
name = "jobs-sg"
version = "0.1.0"
description = "AI Exposure of the Singapore Job Market"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = ["ruff"]
